# alloy_periodic_table
The idea is to show which elements can be added to steel to improve its qualities.
For now it only uses CSS styling
Though I should probably add more info with pop-ups using JS.
